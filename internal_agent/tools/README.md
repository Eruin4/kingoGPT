# internal_agent.tools

This directory is a legacy namespace and currently has no active source files.

Active MVP tools live in:

```text
internal_agent/standalone/tools/
```

Do not add new code here unless you are intentionally promoting tools out of the standalone agent and into a shared runtime package.

